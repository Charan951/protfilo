services:
  - type: web
    name: brian-kabbo-portfolio
    env: docker
    plan: free
    region: singapore # Closest to Bangladesh
    dockerContext: .
    dockerfilePath: ./Dockerfile
    envVars:
      - key: ASPNETCORE_ENVIRONMENT
        value: Production
